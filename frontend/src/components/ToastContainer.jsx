import React from 'react';

// For now just a placeholder to prevent build errors
// We will enhance this later if time permits
const ToastContainer = () => {
  return null;
};

export default ToastContainer;
